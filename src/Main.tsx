import React from 'react'
import ReactDOM from 'react-dom/client'
import DefinitionRoute from './routes/DefinitionRoute.js'
import './styles/index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DefinitionRoute />
  </React.StrictMode>,
)
